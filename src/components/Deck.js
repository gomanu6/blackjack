import React from "react";

const Deck = () => {
	let deck = [];

	let suites = ["Spade", "Heart", "Clubs", "Diamond"];

	let currentCard = {};

	for (let i = 0; (i = suites.length); i++) {
		console.log(i);
	}

	return (
		<div>
			<div></div>
		</div>
	);
};
export default Deck;
